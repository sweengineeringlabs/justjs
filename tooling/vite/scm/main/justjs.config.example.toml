# Example configuration for config codegen
# Copy to justjs.config.toml and customize

[security]
strategy = "oauth"
on_routes = ["/dashboard", "/account"]

[observability]
strategy = "datadog"
all = true

[flags]
strategy = "launchdarkly"

# [analytics]
# strategy = "segment"

# [theming]
# strategy = "runtime"

# [i18n]
# strategy = "intl"
