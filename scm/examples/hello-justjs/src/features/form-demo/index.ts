// Feature barrel — controls what the rest of the app sees for `form-demo`.
//
// Written once by `justw init` / `justw new component`. Edit freely;
// `justw generate app` will not overwrite this file.

// ── Generated surface ─────────────────────────────────────────────
export { FormDemoBase } from "./form_demo_component.gen";

// ── Hand-authored surface ────────────────────────────────────────
// Add your feature's public exports here once you write business logic
// in `./form_demo.ts` or similar.
//
// export { computeRiskScore } from "./form_demo";
