// Feature barrel — controls what the rest of the app sees for `socials`.
//
// Written once by `justw init` / `justw new component`. Edit freely;
// `justw generate app` will not overwrite this file.

// ── Generated surface ─────────────────────────────────────────────
export { SocialsBase } from "./socials_component.gen";

// ── Hand-authored surface ────────────────────────────────────────
// Add your feature's public exports here once you write business logic
// in `./socials.ts` or similar.
//
// export { computeRiskScore } from "./socials";
